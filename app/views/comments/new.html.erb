
<div class="media">
  <div class="media_body">
    <%= comment.body %>
    <% if comment.user> %>
      <small>
          <%= image_tag comment.user.avatar.url(:tiny) if comment.user.avatar? %> submitted <%= time_ago_in_words(comment.created_at) %> ago by <em><%= comment.user.name || comment.user.email %></em>
      </small>
    <% end %>
  </div>
</div>          


